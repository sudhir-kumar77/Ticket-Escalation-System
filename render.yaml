---
# Render Infrastructure-as-Code
# https://render.com/docs/infrastructure-as-code
#
# Services:
#   nvara-db      → Managed PostgreSQL 16
#   nvara-api     → Fastify API (port 4000)
#   nvara-web     → React SPA served via Nginx (proxies /v1/ to API)
#
# Worker is omitted — enable separately when upgrading to a paid tier.

databases:
  - name: nvara-db
    databaseName: nvara_production
    user: nvara
    plan: free
    ipAllowList: []

services:
  # ── API ─────────────────────────────────────────────────────────────────────
  - type: web
    name: nvara-api
    runtime: docker
    dockerfilePath: ./Dockerfile.api
    plan: free
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: DEV_AUTH_ENABLED
        value: "false"
      - key: DATABASE_URL
        fromDatabase:
          name: nvara-db
          property: connectionString
      - key: DEFAULT_ORGANIZATION_NAME
        value: Nvara Media
      - key: PUBLIC_RATE_LIMIT_PER_MINUTE
        value: "60"
      - key: API_PORT
        value: "4000"
      - key: LOG_LEVEL
        value: info
      - key: SLA_POLL_INTERVAL_SECONDS
        value: "60"
      - key: WEB_ORIGIN
        value: https://datavexa.onrender.com
      # ── SMTP (optional) ───────────────────────────────────────────────────
      # Add EMAIL_HOST, EMAIL_USER, EMAIL_PASS, EMAIL_FROM via Render dashboard
      # when you're ready to enable email features (password reset, invitations).

  # ── Web (SPA + Nginx proxy) ──────────────────────────────────────────────────
  - type: web
    name: datavexa
    runtime: docker
    dockerfilePath: ./Dockerfile.web
    plan: free
    envVars:
      - key: API_URL
        value: https://nvara-api.onrender.com
